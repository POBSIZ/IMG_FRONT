import FilterBox from './filterBox';
import SwipeBanner from './swipeBanner';
import TitleBanner from './titleBanner';

const Organisms = {
  SwipeBanner,
  TitleBanner,
  FilterBox,
};

export default Organisms;

export { SwipeBanner, TitleBanner, FilterBox };
